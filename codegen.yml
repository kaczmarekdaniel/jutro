overwrite: true
schema: "https://countries.trevorblades.com/graphql"
documents: "schemas/**/*.graphql"
generates:
  generated/types.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
